本次更新修正白板重點整理被錯誤送進「AI 動作」流程的問題。

- 「你能幫我整理這個白板的重點嗎？」現在會得到一般 AI 回答，不會再要求模型一定提出資料修改。
- 只有明確要求新增、移動、重排、更新、轉換或刪除內容時，才自動進入可預覽、確認後套用的 AI 動作流程。
- 使用者主動開啟 AI 動作後，如果模型認為不需要修改資料但已提供有效摘要，澄境會直接顯示摘要，不再顯示紅色錯誤。
- Gemma 4 與 OpenRouter 使用相同的意圖分流修正；0.7.6 的 Gemma 4 WebGPU 上游效能修正仍完整保留。

Windows 提供 ARM64 與 Intel／AMD x64 安裝程式；Mac 提供 Apple Silicon DMG。Release 只保留三個正式安裝檔。
