# Android 0.10.0-dev.2：介面與操作驗收

這是開發測試版的驗收記錄，不代表完整 Android 正式版已通過所有真機、效能及外部服務驗收。

## 本輪修正

- 導覽採「全部功能」：右上角與底部開啟相同的完整目錄，包含知識庫；不再混用完整清單與剩餘功能。
- 主題、字級、AI 設定、資料庫、知識庫、日誌及白板工具列重新適配窄視窗；行動版文字接上字級比例。
- 同步使用一致的進行中、暫停、待同步與失敗狀態。正常時只保留簡短狀態和時間，衝突以一個可展開的提醒呈現，不顯示內部資料代號。
- 內容相同、僅建立／修改時間不同的舊衝突做一次性合併；截止時間或內容不同仍保留。確認版本前會再檢查資料有沒有變動。
- 一次性資料搬移移至備份與還原區，不再混在日常同步操作裡。
- 第二大腦預設顯示名稱；手機標籤不再隨 3D 距離縮小。桌面顯示策略不變。
- 圖示重用桌面核心標誌，提供 Android 自適應與單色圖層；用像素邊界驗證幾何中心與安全區。
- 頁面、側面板與按壓回饋使用一致節奏，遵守減少動態效果；返回鍵先關閉目前的搜尋、日期選擇器或操作面板。
- 修正從知識庫新增卡片時誤存成片語的預設目標，並保留三種明確儲存位置。
- 外觀、字級與語言額外保存於原生儲存，處理快速改設定後強制關閉導致設定未落盤的情況。
- 移除 Logo 飛入與額外的網頁 Logo 畫面；冷啟動需要準備引擎時使用跟隨主題的原生全畫面開場，工作區準備好立即淡出，不設定最低停留時間。

## 已執行的檢查

|項目|結果與範圍|執行入口／證據|
|---|---|---|
|共用邏輯|130 項通過，包含同步合併、衝突、交易及資料呈現|`npm test`|
|桌面相容邏輯|61 項 Node 測試通過|`npm test`|
|原生備份與圖示|4 項通過：Google 專用測試備份、附件還原與清理、本地保留策略、圖示中心、啟動畫面資源|`scripts/qa-android-native.mjs`|
|設定排版|320／360／411／600／820 寬度 × 三主題 × 100%／120%，共 30 組；檢查選項未被裁掉或橫向溢出|`scripts/qa-android-layout.mjs`|
|逐頁檢視|片語、待辦、日誌、劃記、知識庫、白板、看板、資料庫、第二大腦、設定；截圖與控制項清單，不等同每個分支皆通過|`qa-artifacts/android/ui-audit/`|
|核心觸控操作|11 組流程通過：建立／編輯片語、轉待辦、完成與子待辦、分類面板、建立卡片、立即返回保存、搜尋、資料庫、看板、白板四種節點、日期選擇器返回|`scripts/qa-android-core.mjs`|
|同步狀態|進行中、暫停不冒充完成、失敗、搬移入口分離、隱藏內部代號；同步按鈕內容兩軸置中誤差低於 1 CSS px|`scripts/qa-android-sync-ui.mjs`|
|冷啟動外觀|淺色、深色與跟隨系統深色均確認能恢復主題；錄影與啟動後狀態讀回|`scripts/qa-android-startup.mjs`|

本地錄影、截圖和測試資料保存在 Git 忽略的 `qa-artifacts/android/`。冷啟動腳本中的時間包含模擬器錄影與除錯連線成本，不能當作真機啟動速度或效能基準。

## 測試資料隔離

功能操作測試必須在 Debug 版以 `--ez qa-isolated true` 啟動，並使用獨立 WebView 資料目錄、檔案與設定。該測試環境停用 Google／雲端／同步入口。Release 版忽略此旗標。

執行清理前會記錄既有資料的識別碼，且拒絕刪除測試前既有內容；白板建立需等新識別碼確認後，才登記為測試所有的資料。這修正了早期測試清理的競態問題。當時受影響的模擬器示範內容及三條連線已從保留的操作紀錄還原，並確認同步完成；正式桌面安裝資料與備份未由該清理流程操作。

## 尚不可宣稱完成

- 所有 Android 廠牌、真機注音組字、手勢、長時間操作與發熱。
- 真機本機 Gemma 推論及所有自訂 Provider 的實際回應相容性。
- 大量資料的長期同步日誌壓縮、背景程序被終止後的所有情境、全部附件格式及外部資料夾授權組合。
- 新行動版文案完整五語覆蓋、每個外部社群服務的公開寫入流程。

上述項目延續 `ANDROID.md` 的正式發布關卡，不能用本輪排版或核心操作測試代替。

## 平台依據

- 圖示圖層及遮罩遵循 [Android 自適應圖示文件](https://developer.android.com/develop/ui/compose/system/icon_design_adaptive)。
- 系統控制的啟動過場與自訂退出方式依據 [Android SplashScreen 文件](https://developer.android.com/develop/ui/views/launch/splash-screen)。
- 外觀透過應用層級夜間模式對齊系統啟動畫面，參考 [Android 深色主題文件](https://developer.android.com/develop/ui/views/theming/darktheme)；系統模式的映射另核對 AOSP `UiModeManagerService.setApplicationNightMode` 實作。
