本次更新修正 Mac 視窗縮小時，頂端搜尋說明文字換行並超出邊界的問題。

- 搜尋框現在依主工作區真正可用的寬度調整，不只看整個視窗尺寸。
- 空間開始不足時先隱藏 `⌘K`；更窄時隱藏搜尋文字，只留下固定尺寸的放大鏡按鈕。
- 頂端空間非常有限時，模型狀態與 AI 操作也會收成圖示，避免任何文字互相擠壓。
- 完整搜尋名稱、快捷鍵與螢幕閱讀器支援仍然保留；視窗恢復寬度後會自動展開。
- 0.7.6 的 Gemma WebGPU 效能修正與 0.7.7 的白板 AI 問答修正均完整保留。

Windows 提供 ARM64 與 Intel／AMD x64 安裝程式；Mac 提供 Apple Silicon DMG。Release 只保留三個正式安裝檔。
