# 澄境 0.9.3 健檢報告

日期：2026-09-05。基準：0.9.2。範圍涵蓋本機資料與歷史、備份與附件、AI 與 MCP、搜尋與列表、第二大腦、日常介面，以及更新服務。

## 已發現與修正

| 優先程度 | 問題與影響 | 修正 |
| --- | --- | --- |
| 高 | 資料庫交易最後取消，先前已執行的 hooks 仍留下歷史，造成錯誤 Undo | 只記錄單筆成功且整筆交易提交的變更；取消交易不進入歷史 |
| 高 | 背景維護期間的全域歷史暫停可能吞掉使用者操作 | 改為交易自己的排除標記；其他操作正常記錄 |
| 高 | 同名、大小寫不同或 Windows 名稱衝突的筆記，Markdown 匯出只剩一份 | 自動產生不碰撞的檔名；移除逐位元組組字串的 ZIP 複製 |
| 高 | 備份只通過外殼格式檢查，就能清空目前資料 | 檢查必要資料表、記錄 ID、重複鍵及核心筆記格式；本地復原先建立安全副本 |
| 高 | 備份附件可能在整份復原失敗前覆寫同名原檔 | 使用獨立目的檔，串流核對內容；失敗清除新檔，原檔不變 |
| 高 | 移除附件後 Undo 只能找回資料列，實體檔已刪除 | 延後檔案清理至下一次啟動；只清理仍未被引用的待移除項目 |
| 高 | MCP 追加文字會重建整篇 HTML，丟失格式與連結 | 只將新文字轉成內容，接到原 HTML 後面 |
| 高 | MCP 的版本檢查和寫入分開，兩個相同版本請求可能都通過 | 檢查與整組寫入共用交易；並發測試只有一方成功 |
| 高 | MCP 建立子待辦可能漏掉母項目狀態；重新同步遺失父子欄位 | 重用既有子待辦流程，並保留已有關聯欄位 |
| 高 | 切換卡片後，較晚回來的 AI 動作計畫可能出現在新脈絡 | 請求綁定原脈絡；過時的 UI 更新失效，已完成普通回覆仍保存於原對話 |
| 高 | Provider 失敗後重試，可能使用中途切換的新連線 | 一次請求與重試固定綁定同一 Provider |
| 中 | 搜尋先截斷候選，再判斷符合，並漏掉未進入字詞索引的內容 | 先過濾再限制結果；索引不足時以有界結果的 cursor 補找單字元、全半形、字內片段與長文尾端 |
| 中 | 查詢結果與數量統計建立整批候選陣列 | 可見結果保持上限，計數改用 cursor；核心列表批次顯示繼續保留 |
| 中 | 待辦數量使用不能正確索引的布林欄位 | 使用已有的 `doneKey`；排除垃圾桶關聯待辦，數量與畫面一致 |
| 中 | 第二大腦無操作時仍連續繪製；移動時反覆建立向量 | 按需繪製並重用向量。需要動畫時仍持續更新 |
| 中 | 每次卡片任意屬性變更都重建全文索引；PDF 解析工作未明確釋放 | 只有搜尋來源欄位變更才重建索引；PDF 匯入後結束解析工作 |
| 中 | 模型列得出來，不代表可以回答；未保存的設定可能被誤認為已測試 | 加入「試送訊息」，未保存時停用測試並顯示原因 |
| 中 | HTTP 400、401、429、503 全被稱為連不上服務 | 分別顯示設定、授權、額度及伺服器錯誤；保留服務的具體原因 |
| 中 | 結構化輸出發生任意錯誤都重試，可能重複消耗額度 | 只有明確不支援格式時降級；temperature 快取綁定實際位址並限制容量 |
| 中 | 同時保存多組 Provider 可能最後寫入者覆蓋其他設定 | 設定修改依序處理；加密寫入使用唯一暫存檔名 |
| 中 | 大附件上傳期間，另一台裝置可能已更新雲端版本 | 發布快照前再查一次雲端版本；待決衝突不執行快照清理 |
| 中 | 更新服務的備援資料可能降低版本或丟棄既有檔案驗證資料 | 合併時優先較新版本，保留同檔案且大小相符的驗證資料 |
| 中 | 啟動資料錯誤可能永遠停在載入畫面 | 提供清楚錯誤狀態、詳情及重新載入操作 |

## 效能證據

在同一台 Apple Silicon Mac、獨立測試資料下觀察未選取、未啟用共享動畫的第二大腦。實際已安裝的對照版是 0.9.1，其第二大腦繪圖程式與 0.9.2 相同：

| 量測 | 0.9.1 已安裝對照 | 0.9.3 修改版 |
| --- | ---: | ---: |
| 靜止一秒的 WebGL 繪圖呼叫 | 15,120 | 0 |
| WASD 移動後視野改變 | 正常 | 正常 |

這是繪圖呼叫的量測，不是耗電量或所有設備的 FPS 保證。共享神經元的動畫、選取脈動與操作時仍會重畫。做法採用 [React Three Fiber 的按需繪圖方式](https://r3f.docs.pmnd.rs/advanced/scaling-performance)。

啟動初測三輪，新版／對照版畫面就緒中位數為 662／601ms，單輪波動較大；保留這批原始資料後，以同一時段各七輪再測：新版／對照版為 655／674ms，首次操作為 677／692ms，RSS 為 760／754MiB，閒置計時延遲均為 0ms。結果未顯示穩定的啟動退步，也不將毫秒差異宣稱為顯著加速。

1,200 張卡片壓測繼續檢查：私人神經元最多顯示 200 個、移動後換入不同項目、遠端共享節點保持可見、完整搜尋可定位未顯示內容、歷史僅保存變更記錄。未更換資料庫格式。

## 功能與設計判斷

本次適合補上的功能是「模型實際回覆測試」與「啟動失敗可重新載入」。前者處理已出現的服務可連線但模型無法回答情境，後者提供載入失敗時的明確出口。

Design Read：既有桌面工具介面延伸；服務一般長期筆記使用者。保留實心色階、短標籤與低干擾回饋，視覺變異 2/10、動態 2/10、資訊密度 6/10、品牌一致性 10/10。沿用既有色票、12px 以上介面文字、4px 間距單位與 6–10px 控制圓角。新增的回覆測試使用一行說明和一個動作，未增加新的設定頁。

## 驗證方式與限制

- `npm test`：103 個前端／資料邏輯測試，54 個 Electron 服務測試。
- `qa:health-suite`：33 組既有日常工作流程；另有 `qa:health` 檢查真實 Electron 繪圖、視野移動及附件讀回。
- 33 組均已通過；修正舊版測試的示範資料筆數、日期、白板開啟方式與資料載入等待。重試紀錄保存在情境結果中，沒有忽略失敗。
- 型別與語法檢查、正式建置、Windows 雙架構主程式與解除安裝程式、正式 Mac App、DMG 驗證。
- 更新 Worker 有 5 個契約測試、型別檢查與部署預演；依 [Cloudflare 官方最佳實務](https://developers.cloudflare.com/workers/best-practices/workers-best-practices/)處理快取與背景工作。
- 正式 Worker 版本 `a0f2a40c-7a83-4a9e-99ee-d4746da65c46` 已部署，公開備援回應保留三個平台的檔案驗證資料。
- 交易事件依 [Dexie 的完成事件](https://dexie.org/docs/Transaction/Transaction.on.complete)記錄，並測試取消交易及並發寫入。
- 公司 Gateway、每個模型、所有 Windows 實體機無法逐一涵蓋；目前 Provider 測試使用可控制錯誤回覆的測試服務。Windows x64 為介面與封裝檢查，沒有宣稱已在 x64 實體機上實測。

## 下一階段值得投入

1. **多設備同步的完整衝突介面。** 現在是工作區快照備份與版本檢查，仍不等於多人同時編輯、自動合併。發布前再次檢查能縮小上傳期間的衝突窗口；真正跨設備原子協調仍需要完整同步協議及多設備壓測。
2. **萬筆以上資料的背景摘要投影。** 第二大腦已限制繪製數量，但建立完整關係圖仍會讀取全部相關記錄；大型完整 JSON 匯出也仍會暫時持有工作區快照。下一階段可將摘要與全文索引分段持久化，交給背景 Worker 更新，再以增量協議搬運備份。
3. **發行簽章。** Windows Authenticode、macOS Developer ID 與公證能改善首次安裝的信任體驗；目前沒有可用的正式簽章身分，因此本次沿用既有發行方式。

這些項目需獨立的資料遷移、同步契約或發行身分；本次先修正已重現的問題，並保留現有資料與操作的相容性。
