這一版讓大型待辦可以真正拆成有層級、會互相反映完成狀態的子項目，而不是只能在標題裡自行編號。

- 每一筆待辦的右鍵選單新增「新增子項目」；使用輕巧輸入浮層，不離開目前的待辦時間軸。
- 子項目會顯示在主項目下方，以細縮排線、半階背景與「子項目」標籤形成清楚層級，不加入厚重樹狀框。
- 主項目顯示整棵子樹的完成進度，例如 `2/4 子項目完成`；視覺縮排最多四階，資料仍可支援更多層級而不把文字擠壞。
- 最後一個子項目完成時，主項目會自動完成；任一子項目重新開啟時，所有上層項目會自動重新開啟。
- 直接完成主項目代表完成整個群組，會一併完成其所有下層項目；手動重新開啟主項目則保留已完成的子項目紀錄。
- 在已完成主項目下新增新子項目時，主項目會自動重新開啟；相同的未完成直接子項目不會重複建立。
- 子項目沿用主項目的來源卡片與初始期限，之後仍可個別修改；來自卡片或日誌 Checkbox 的主項目，也會把自動完成／重新開啟同步寫回原編輯器。
- 刪除含有子項目的主項目前會明確顯示子樹數量並確認；同意後會清理整棵子樹及對應第二大腦連線。
- 第二大腦會以不可誤刪的結構線連接主項目與子項目，不需要 AI 猜測這個明確關係。
- 今日與日誌側欄只顯示主項目，避免子項目脫離上下文重複出現；資料庫與搜尋仍能找到每一筆子項目。
- 新增完整多層待辦 QA；自動完成、重新開啟、主項目整組完成、Checkbox 回寫、刪除子樹、1100px／120% 字級與既有待辦時間軸均通過。
