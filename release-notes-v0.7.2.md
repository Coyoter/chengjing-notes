這一版把 PDF 卡片頂端無法操作的白色區塊，改成真正可閱讀、可操作、也能安心降級的本機文件預覽。

- 移除在澄境安全附件協定上無法正常工作的 Chromium PDF `<embed>`；不再留下不可點擊、不可編輯的白色空白區塊。
- PDF 卡片頂端改為安靜的文件來源卡：清楚顯示檔名、頁數、容量、儲存副本與移除附件。
- 使用已隨澄境打包的 PDF.js，把第一頁真正渲染成 Canvas；預覽區會顯示原始 PDF 內容，而不是模擬縮圖或白色 placeholder。
- 點擊第一頁即可在澄境中央開啟完整 PDF 閱讀器，不跳瀏覽器，也不取代右側 AI；關閉後回到原卡片與原閱讀位置。
- 完整閱讀器支援上一頁／下一頁、直接輸入頁碼、方向鍵與 Page Up／Page Down 換頁、縮放及符合寬度。
- 可以把原始 PDF 另存副本；移除附件時只移除原檔預覽，已擷取、可搜尋與可編輯的文字會完整保留。
- 密碼保護、損壞或暫時無法解析的 PDF 會顯示清楚的低干擾文件卡；文字內容與儲存副本仍可使用，不再退回空白區塊。
- PDF 執行核心採按需載入：只有匯入或打開 PDF 時才載入，不增加一般啟動與其他筆記頁面的負擔。
- 淺色、深色、墨色、五語、1100px 視窗與 120% 字級均完成驗收；正式 PDF 匯入、三頁換頁、鍵盤、縮放、失敗降級與移除附件測試全數通過。
