這一版讓「我想改一下」成為每種內容都找得到的基本操作，也把單純翻閱日期留下的空白日誌，從真正的知識內容中分開。

- 卡片、看板卡片、待辦、隻言片語、劃記與白板都新增一致的右鍵「編輯」，並放在選單第一項。
- 卡片型內容沿用完整編輯頁；待辦、片語與劃記使用輕巧、低干擾的編輯浮層；白板文字與區段直接留在原處編輯。
- 從看板右鍵編輯卡片後，關閉仍回到原看板，不會失去剛才的工作脈絡。
- 編輯由卡片 Checkbox 產生的待辦時，文字會同步寫回原卡片，不會出現兩份不同內容。
- 只打開日期、不輸入內容也不修改標題的日誌，現在不會出現在卡片庫、資料庫、搜尋、第二大腦或 AI 本機內容範圍。
- 一旦真正寫入日誌或修改標題，它會立即成為正式內容；曾經寫過再清空的日誌也不會被誤刪。
- 舊版累積的空白日誌會先安全辨識，沒有標籤、附件、待辦、白板位置、看板位置或神經元關係時，才會在背景清理。
- 新增完整右鍵編輯旅程與空白日誌驗收；第二大腦大資料量、待辦同步、白板、看板與知識架構回歸均通過。

首次開啟 0.7.1 時會進行一次輕量資料辨識；既有有內容的日誌、卡片、待辦、白板、看板與共享資料都會保留。
