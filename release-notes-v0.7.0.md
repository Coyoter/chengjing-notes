這一版替澄境補上長期使用需要的資料與效能地基；功能與設計不縮水，資料量增加時也不再用「全部讀取、全部渲染」硬撐。

- 全域「上一步／下一步」改為差異操作紀錄。一次操作只保存真正新增、修改或刪除的資料列，不再每次複製整座資料庫；白板的快速復原也補上連續操作競態保護。
- 私人第二大腦採視野分區：同時最多渲染 200 顆自己的神經元，移動鏡頭會平滑換入附近內容並卸載離開視野的舊內容。搜尋仍檢查完整私人資料，命中後鏡頭會柔和移向目標。
- 陌生人的共享神經元維持原契約：每次最多 20 顆、全部常駐顯示，不計入私人 200 顆上限，也不因鏡頭距離而被卸載。
- 卡片庫、資料庫、待辦、隻言片語與劃記加入分批顯示；卡片與待辦搜尋先走本機多語索引，不再每次鍵入都把全部內文掃進畫面。
- 白板只載入目前白板實際使用的卡片；大量內容優化不採會破壞復原顯示的激進可視區策略，維持原本可靠交互。
- 新附件改存澄境專屬檔案資料夾；資料庫只保存名稱、路徑、大小與 SHA-256。圖片、PDF、影音不再直接膨脹筆記資料庫；舊版 IndexedDB Blob 會在閒置時安全遷移。
- 自動備份升級為內容雜湊增量資產庫：相同附件只複製一次，JSON 保存完整結構，過期備份與已無引用資產會由澄境安全整理。舊版完整 JSON 仍可匯入。
- 設定新增「本機空間分佈」，分別顯示筆記與結構、附件檔案、本機 AI 模型，不再把 2.9 GB 模型誤認成筆記膨脹。
- 卡片版本歷史改為稀疏保留：最新 30 份完整保留，近一年每日一份，更久以前每月一份；不再永久累積每三分鐘版本。
- 啟動時不再同步掃描每張卡片。只處理待同步的 Checkbox 卡片，並在畫面可操作後利用空閒時間整理版本與舊附件。
- 第二大腦、白板、PDF、編輯器與設定改為真正需要時才載入；主啟動 JavaScript 由約 3.24 MB 降到約 572 KB。
- 新增長期資料壓測：1,200 張卡片、400 筆片語、400 個待辦、300 筆劃記下，私人視野固定 200 顆、20 顆陌生共享完整顯示，所有列表維持固定首批渲染上限。

這次資料庫會在首次開啟 0.7.0 時進行一次相容升級；既有筆記、白板、待辦、連線、附件、共享身分與備份設定都會保留。
